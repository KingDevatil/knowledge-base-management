# Knowledge Base Management Nginx template placeholder.
#
# Docker compose mounts this file for operators who want to inspect or replace
# the generated gateway configuration. The active container configuration is
# rendered by /etc/nginx/templates/render-nginx-conf.sh so pure-internal
# deployments can start without external-domain TLS certificates.
